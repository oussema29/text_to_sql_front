import { Component } from '@angular/core';

/** Placeholder — built in build-order step 5 (see BUILD_PLAN.md). */
@Component({
  selector: 'app-audit-log',
  standalone: true,
  template: `<div style="padding:32px; color:#5F6373;">Journal d'audit — étape 5, à construire.</div>`
})
export class AuditLogComponent {}