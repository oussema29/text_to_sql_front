import { Component } from '@angular/core';

/** Placeholder — built in build-order step 3 (see BUILD_PLAN.md). */
@Component({
  selector: 'app-query-workspace',
  standalone: true,
  template: `<div style="padding:32px; color:#5F6373;">Espace de requête — étape 3, à construire.</div>`
})
export class QueryWorkspaceComponent {}