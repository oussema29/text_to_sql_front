import { Component } from '@angular/core';

/** Placeholder — built in build-order step 4 (see BUILD_PLAN.md). */
@Component({
  selector: 'app-table-detail',
  standalone: true,
  template: `<div style="padding:32px; color:#5F6373;">Détail de table — étape 4, à construire.</div>`
})
export class TableDetailComponent {}
