import { Component } from '@angular/core';

/** Placeholder — built in build-order step 4 (see BUILD_PLAN.md). */
@Component({
  selector: 'app-table-list',
  standalone: true,
  template: `<div style="padding:32px; color:#5F6373;">Explorateur de schéma — étape 4, à construire.</div>`
})
export class TableListComponent {}
