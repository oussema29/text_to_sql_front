import { Component } from '@angular/core';

/** Placeholder — built in build-order step 6 (see BUILD_PLAN.md). */
@Component({
  selector: 'app-document-list',
  standalone: true,
  template: `<div style="padding:32px; color:#5F6373;">Métadonnées du schéma — étape 6, à construire.</div>`
})
export class DocumentListComponent {}
